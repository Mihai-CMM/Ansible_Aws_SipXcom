#!/usr/bin/env python
import json, requests, yaml

output_json =  yaml.load(open('stefan.json'))  
headers = {'content-type': 'application/json'}
for i in output_json:
	for k in output_json[i]:
                       print k
                      # 	r = requests.post("https://superadmin:Ezuce123@10.3.0.150/sipxconfig/api/users/", data=k,verify=False,headers=headers)
                     #   print r.status_code

